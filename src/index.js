import React from 'react'; //  creating and managing components
import ReactDOM from 'react-dom'; // rendering React components in the browser.

import App from './App';
import './index.css';

ReactDOM.render(<App />, document.getElementById('root'));
// tells React to render the App component into the HTML element with the id 'root.'